# Template Environment Configuration for ALIGN.AI
# Copy this file to .env.local for development or .env.production for production

# Application Base URL (Used for dynamic origin resolution, metadata, and links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# External API URL (Leave empty if using Next.js relative API routes like /api/...)
NEXT_PUBLIC_API_URL=
